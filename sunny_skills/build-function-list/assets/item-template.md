# F-xxx <機能名>

- 区分: <区分>
- 状態: **<確定 / 一部未確定 / 未確定 のいずれか>**
- 根拠 REQ: <この機能へ対応づけた REQ-xxx を読点で並べる。一覧の同じ行の値と一致させる>
- 関連 QA（未解決）: <work/qa/items/ に実在する Q-xxx へのリンク。無い場合は「なし」と書く>

`- 状態:` の行を消してはならない。
理由: `work/tools/check_functions.py` はこの行を読み、一覧の状態と一致するかを検査する。

## 目的

<この機能が何を実現するかを 1 段落で書く。原文に無い目的を書いてはならない>

## 入力

<この機能が受け取るものを書く。該当が無い場合は「該当なし」と書く>

## 出力

<この機能が出すものを書く。該当が無い場合は「該当なし」と書く>

## 振る舞い（正常系）

<番号付きの手順で書く。原文に無い手順を補ってはならない>

## 振る舞い（異常系）

<事象・対応・根拠の 3 列の表で書く。該当が無い場合は「該当なし」と書く>

| 事象 | 対応 | 根拠 |
|---|---|---|

## 制約（要求由来）

要求そのものが実装方式を拘束している事項だけを書く。
こちらの設計判断を書いてはならない。
理由: 両者が混ざった場合、後から「これは要求か、こちらの案か」を分離できない。

該当が無い場合は「該当なし」と書く。

| # | 制約 | 根拠 REQ |
|---|---|---|

## 未確定事項

各行に種別を必ず付ける。種別は `QA待ち` / `解釈（要確認）` / `原本改訂で変わりうる` の 3 つとする。
該当が無い場合は「該当なし」と書く。

`追跡` 欄には、実在する `Q-xxx` へのリンクを書く。
QA doc がまだ無い論点は、元の論点 ID とノート名で書く。形は「`<主題>` 解析ノート `A-02`（未起票）」とする。
工程 4 の衝突チェックで出た行は、組の 2 つの `F-ID` で書く。形は「衝突チェック `F-012`×`F-031`（未起票）」とする。
`Q-xxx` を推測して書いてはならない。理由: `work/tools/check_functions.py` が `UNKNOWN_QID` で止まる。

| # | 内容 | 種別 | 追跡 |
|---|---|---|---|

## トレース

- 解析ノート: <解析ノートへのリンクと、根拠になった解釈の番号。書く番号の範囲は SKILL.md の工程 5「トレースに書く解釈の番号」による>
- 関連機能: <他の F-xxx へのリンク。無い場合は「なし」と書く>

## 更新履歴

| 日付 | 変更点 | きっかけ |
|---|---|---|
| <YYYY-MM-DD> | 新規作成 | `build-function-list` スキルの実行 |
